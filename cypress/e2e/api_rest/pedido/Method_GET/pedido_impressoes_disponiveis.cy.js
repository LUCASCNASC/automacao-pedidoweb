

describe('/v3/pedido_impressoes_disponiveis/{idFilial}/{idPedidoVenda}', () => {

    it('200 - Deve realizar a ação corretamente',() => {
      cy.request({
        method: 'GET',
        url: 'http://localhost:8000/Pedido/v3_pedido_impressoes_disponiveis',
        body: {
                
              }
      }).then((response)=>{
          expect(response.status).to.equal(200); //Verifica status da resposta

      })
    });  
});