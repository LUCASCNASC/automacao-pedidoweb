

describe('/v3/contrato_servico/{filial}/{pedido}', () => {

    it('200 - Deve realizar a ação corretamente',() => {
      cy.request({
        method: 'GET',
        url: 'http://localhost:8000/Pagamento%20pedido/v2_pag_pedido_contrato_servico',
        body: {
                
              }
      }).then((response)=>{
          expect(response.status).to.equal(200); //Verifica status da resposta

      })
    });  
});