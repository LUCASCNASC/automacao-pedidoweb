

describe('/v3/dividas_a_receber/{idFilial}/{cpf_cnpj}', () => {

    it('200 - Deve realizar a ação corretamente',() => {
      cy.request({
        method: 'GET',
        url: 'http://localhost:8000/Financeiro/v3_financeiro_dividas_a_receber',
        body: {
                
              }
      }).then((response)=>{
          expect(response.status).to.equal(200); //Verifica status da resposta

      })
    });  
});