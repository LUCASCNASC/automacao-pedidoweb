

describe('/v3/apaga_usuario_inventario/{idFilial}/{idInventario}', () => {

    it('200 - Deve realizar a ação corretamente',() => {
      cy.request({
        method: 'POST',
        url: 'http://localhost:8000/Inventário/v3_post_apaga_usuario_inventario',
        body: {
                
              }
      }).then((response)=>{
          expect(response.status).to.equal(200); //Verifica status da resposta

      })
    });  
});