

describe('/v3/carregar_itens_nota_transferida_coletor', () => {

    it('200 - Deve realizar a ação corretamente',() => {
      cy.request({
        method: 'POST',
        url: 'http://localhost:8000/Logística/v3_get_carregar_Itens_nota_transferida_coletor',
        body: {
                
              }
      }).then((response)=>{
          expect(response.status).to.equal(200); //Verifica status da resposta

      })
    });  
});