

describe('/v3/indicador_vendedor_periodo/{datainicial}/{datafinal}', () => {

    it('200 - Deve realizar a ação corretamente',() => {
      cy.request({
        method: 'GET',
        url: 'http://localhost:8000/Indicadores/v3_indicador_vendedor_periodo',
        body: {
                
              }
      }).then((response)=>{
          expect(response.status).to.equal(200); //Verifica status da resposta

      })
    });  
});